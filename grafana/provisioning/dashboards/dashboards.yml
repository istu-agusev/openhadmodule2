apiVersion: 1

providers:
  - name: "IoT Dashboards"
    folder: "Greenhouse"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10

    options:
      path: /var/lib/grafana/dashboards